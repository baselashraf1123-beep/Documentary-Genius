# دليل التشغيل الكامل — Documentary Genius

هذا الدليل يشرح الخطوات المتبقية لتشغيل النظام فعلياً على أرض الواقع. كل ما هو "كود" تم إنجازه وتجربته؛ الخطوات هنا هي فقط ما يتطلب حساباتك الشخصية (استضافة، GitHub، فيسبوك) والتي لا يمكن لأي مساعد أن ينفّذها نيابة عنك.

---

## 1) تشغيل الخادم الخلفي (Backend)

الخادم هو "العقل" الذي ينتج الحلقات فعلياً — يحتاج تشغيلاً دائماً على خادم حقيقي (وليس على جهازك فقط، لأن التطبيق سيتصل به من هاتفك).

### الطريقة الأسهل: أي استضافة تدعم Docker
أمثلة: Railway، Render، Fly.io، أو أي VPS عادي (Hetzner، DigitalOcean، Contabo...).

على الخادم:
```bash
git clone https://github.com/baselashraf1123-beep/Documentary-Genius.git
cd Documentary-Genius/backend
cp .env.example .env
python3 -c "import secrets; print(secrets.token_hex(32))"   # الصق الناتج في SECRET_KEY داخل .env
cd ..
docker compose up -d --build
```
سيعمل الخادم على المنفذ `5000`. عنوانك النهائي سيكون شيئاً مثل `https://your-domain.com` أو `http://SERVER_IP:5000` حسب استضافتك.

### أول تشغيل: احفظ كلمة المرور المولَّدة
عند أول إقلاع، ستظهر في سجلّات الخادم (logs) رسالة تحتوي اسم مستخدم وكلمة مرور عشوائية — **هذه هي المرة الوحيدة التي ستظهر فيها**. احفظها فوراً، وغيّرها من داخل التطبيق (الإعدادات ← تغيير كلمة المرور) بمجرد تسجيل الدخول أول مرة.

عرض السجلّات: `docker compose logs backend`

---

## 2) بناء تطبيق الهاتف (APK)

جهّزت ملف GitHub Actions يبني لك APK موقّعاً تلقائياً — لا حاجة لتثبيت Flutter على جهازك.

### إعداد لمرة واحدة
من صفحة مستودعك على GitHub: **Settings → Secrets and variables → Actions → New repository secret**، أضف 4 أسرار (القيم موجودة في ملف `android/key.properties` لديك محلياً):

| اسم السر | من أين تحصل عليه |
|---|---|
| `ANDROID_KEYSTORE_BASE64` | نفّذ: `base64 -w0 android/release-key.jks` وانسخ الناتج |
| `ANDROID_STORE_PASSWORD` | قيمة `storePassword` في `key.properties` |
| `ANDROID_KEY_PASSWORD` | قيمة `keyPassword` في `key.properties` |
| `ANDROID_KEY_ALIAS` | قيمة `keyAlias` في `key.properties` |

### التشغيل
ادفع الكود لفرع `main` على GitHub (أو من تبويب **Actions** اضغط **Run workflow** يدوياً). بعد حوالي 3-5 دقائق، ستجد ملف APK جاهزاً للتحميل من نفس الصفحة تحت "Artifacts".

### التثبيت على الهاتف
حمّل ملف الـ APK على هاتفك، وفعّل خيار "تثبيت من مصادر غير معروفة" (Install unknown apps) عند الطلب، ثم ثبّته عادياً.

> ⚠️ ملاحظة أمان: احتفظ بملف `release-key.jks` وكلماته في مكان آمن خاص بك (خارج أي مستودع). أي تحديث مستقبلي للتطبيق يجب توقيعه بنفس المفتاح، وإلا لن يقبل النظام تثبيت التحديث فوق النسخة القديمة.

---

## 3) الربط الأول من داخل التطبيق

1. افتح التطبيق ← "إعدادات الاتصال بالخادم" (تظهر في شاشة الدخول)
2. الصق عنوان خادمك من الخطوة 1، ثم اضغط "اختبار الاتصال"
3. سجّل الدخول بالبيانات التي حفظتها من سجلّات الخادم
4. من الإعدادات: غيّر كلمة المرور فوراً

---

## 4) تفعيل النشر التلقائي على صفحة فيسبوك

هذه الخطوة الوحيدة التي تحتاج حساب فيسبوك المطوّر الخاص بك — لا يمكن لأي طرف خارجي إنشاءها نيابة عنك لأسباب أمنية.

1. اذهب إلى developers.facebook.com ← **My Apps** ← **Create App** ← اختر نوع **Business**
2. من لوحة التطبيق، افتح **Graph API Explorer**
3. اختر تطبيقك من القائمة، ثم من "Permissions" أضف:
   `pages_show_list`, `pages_manage_posts`, `pages_read_engagement`
4. اضغط **Generate Access Token** ووافق على الأذونات لصفحتك
5. بدّل الرمز المؤقت إلى رمز طويل الأمد عبر نقطة `/oauth/access_token` (الأداة تشرحها في نفس الصفحة)، ثم من نقطة `/me/accounts` احصل على **access_token الخاص بصفحتك تحديداً** (وليس رمز حسابك الشخصي) — هذا هو المطلوب هنا
6. من إعدادات صفحة الصفحة نفسها احصل على **Page ID** (يظهر أسفل اسم الصفحة أو من About)
7. داخل التطبيق: الإعدادات ← قسم "صفحة فيسبوك" ← الصق Page ID والرمز ← اضغط "اختبار الاتصال" فإن نجح، اضغط "حفظ"

بعدها، كل حلقة تنتجها ستظهر بها زر "نشر على فيسبوك" في صفحة تفاصيلها.

> ملاحظة: طالما أنت المدير على الصفحة والتطبيق في وضع Development، هذا الرمز يعمل بلا حاجة لمراجعة (App Review) من Meta، لأنك تنشر على صفحتك أنت فقط.

---

## ملخص ما تم تنفيذه في الكود (للمرجعية)

- إصلاحات أمان: مفتاح جلسات آمن، تشفير كلمات مرور حديث بترقية تلقائية للحسابات القديمة، إزالة كلمة المرور الافتراضية الثابتة
- ميزة جديدة كاملة: نشر تلقائي على فيسبوك (خادم + واجهة)، مع تتبّع حالة كل نشر
- شاشة تغيير كلمة المرور
- إعداد Docker + docker-compose جاهز للنشر بأمر واحد
- GitHub Actions لبناء APK موقّع تلقائياً دون تثبيت أي شيء محلياً
- `DB_PATH` قابل للضبط عبر متغيّر بيئة حتى لا تُفقد البيانات عند إعادة بناء الحاوية
