# انسخ هذا الملف إلى .env واملأ القيم قبل التشغيل
# cp .env.example .env

# مفتاح سري لتوقيع الجلسات — أنشئ واحداً عشوائياً بهذا الأمر ثم الصقه هنا:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# المنفذ الذي يستمع عليه الخادم (لا تغيّره عادة إلا لو استضافتك تفرض منفذاً معيناً)
PORT=5000
